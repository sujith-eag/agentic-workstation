{#- Artifact Table Partial -#}
{#- Displays tiered artifact tables (core/domain/log) grouped by category -#}

{#- Parameters:
    produces: dict with keys: core, domain, log (each is array of {filename, description})
    consumes: dict with keys: core, domain (each is array of {filename, description})
    show_ownership: boolean - whether to show ownership column
-#}

## Artifact Ownership

{% if produces %}
### Produces

{% if produces.core %}
#### Core Artifacts
| Filename | Description |
|----------|-------------|
{% for artifact in produces.core %}
| `{{ artifact.filename }}` | {{ artifact.description | default('-') }} |
{% endfor %}
{% endif %}

{% if produces.domain %}
#### Domain Artifacts
| Filename | Description |
|----------|-------------|
{% for artifact in produces.domain %}
| `{{ artifact.filename }}` | {{ artifact.description | default('-') }} |
{% endfor %}
{% endif %}

{% if produces.log %}
#### Log Artifacts
| Filename | Description |
|----------|-------------|
{% for artifact in produces.log %}
| `{{ artifact.filename }}` | {{ artifact.description | default('-') }} |
{% endfor %}
{% endif %}
{% endif %}

{% if consumes %}
### Consumes

{% if consumes.core %}
#### Core Artifacts
| Filename | Description |
|----------|-------------|
{% for artifact in consumes.core %}
| `{{ artifact.filename }}` | {{ artifact.description | default('-') }} |
{% endfor %}
{% endif %}

{% if consumes.domain %}
#### Domain Artifacts
| Filename | Description |
|----------|-------------|
{% for artifact in consumes.domain %}
| `{{ artifact.filename }}` | {{ artifact.description | default('-') }} |
{% endfor %}
{% endif %}
{% endif %}

{% if not produces and not consumes %}
*No artifacts defined for this agent.*
{% endif %}
