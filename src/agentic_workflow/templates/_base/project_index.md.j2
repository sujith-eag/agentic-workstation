{#
  Project Index Template
  
  This template generates the project_index.md file.
  
  Required context:
    - project_name: Project name
    - workflow_name: Workflow ID
    - workflow_display_name: Display name
    - workflow_version: Version string
    - timestamp: Creation timestamp
    - registry_entries: List of {name, owner, description}
    - current_phase: Current workflow phase
    - active_agent: Currently active agent
    - last_action: Last recorded action
#}
# Project Index: {{ project_name }}

> **{{ workflow_display_name }}** v{{ workflow_version }}  
> Created: {{ timestamp }}

---

## File Registry

{% if registry_entries %}
{% for entry in registry_entries %}
- **{{ entry.name }}** ({{ entry.owner }}): {{ entry.description }}
{% endfor %}
{% else %}
> (No artifacts registered yet)
{% endif %}

---

## Topic Map

> (Empty - Will be populated by Orchestrator)

---

## Status

| Property | Value |
|----------|-------|
| **Current Phase** | {{ current_phase }} |
| **Active Agent** | {{ active_agent }} |
| **Last Action** | {{ last_action }} |

---

## Quick Links

| Resource | Path |
|----------|------|
| Active Session | `agent_context/active_session.md` |
| Exchange Log | `agent_log/exchange_log.md` |
| Context Log | `agent_log/context_log.md` |
| CLI Reference | `docs/CLI_REFERENCE.md` |
| Agent Pipeline | `docs/AGENT_PIPELINE.md` |

---

*Last updated: {{ timestamp }}*
